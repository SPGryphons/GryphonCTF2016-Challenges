service oracleservice
{
    disable = no
    socket_type = stream
    protocol    = tcp
    wait        = no
    user        = oracleuser
    bind        = 0.0.0.0
    server      = /home/oracleuser/oracle.py
    type        = UNLISTED
    port        = 31337
}

